<?php

namespace GraphQL\SchemaObject;

class DisplayableNameHeightPropertyQueryObject extends QueryObject
{
    const OBJECT_NAME = "DisplayableNameHeightProperty";

    public function selectLanguage(DisplayableNameHeightPropertyLanguageArgumentsObject $argsObject = null)
    {
        $object = new DisplayableLanguageQueryObject("language");
        if ($argsObject !== null) {
            $object->appendArguments($argsObject->toArray());
        }
        $this->selectField($object);

        return $object;
    }

    public function selectValue(DisplayableNameHeightPropertyValueArgumentsObject $argsObject = null)
    {
        $object = new MarkdownQueryObject("value");
        if ($argsObject !== null) {
            $object->appendArguments($argsObject->toArray());
        }
        $this->selectField($object);

        return $object;
    }
}
